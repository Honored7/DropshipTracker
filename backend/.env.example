# DropshipTracker backend environment variables
# Copy to .env and edit before running.

# Server port (default 8000)
PORT=8000

# Set to "true" to enable verbose Scrapling debug logging
SCRAPLING_DEBUG=false
